# Large dataset/model files (excluded from git — keep locally only)
*.h5
*.pth
*.npz
*.pt
*.tar.gz

# Keep smaller files that are under GitHub's 100MB limit
!coco_captioning/coco2014_captions.h5
!**/emoji_data.npz
!**/text_embeddings.pt

# Python cache
__pycache__/
*.pyc

# Claude Code settings (local only)
.claude/
